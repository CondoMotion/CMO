<!DOCTYPE html>
<html lang="en">
	<head>
    <meta charset="utf-8">
    <title>Condo Motion</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Le styles -->
    <%= stylesheet_link_tag    "application", :media => "all" %>
  	<link href='http://fonts.googleapis.com/css?family=Raleway:100|Varela' rel='stylesheet' type='text/css'>
  	<%= javascript_include_tag "application" %>
  	<%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <!-- Le fav and touch icons -->
    <link rel="shortcut icon" href="../assets/ico/favicon.ico">
    <link rel="apple-touch-icon-precomposed" sizes="144x144" href="../assets/ico/apple-touch-icon-144-precomposed.png">
    <link rel="apple-touch-icon-precomposed" sizes="114x114" href="../assets/ico/apple-touch-icon-114-precomposed.png">
    <link rel="apple-touch-icon-precomposed" sizes="72x72" href="../assets/ico/apple-touch-icon-72-precomposed.png">
    <link rel="apple-touch-icon-precomposed" href="../assets/ico/apple-touch-icon-57-precomposed.png">
  </head>

  <body>

    <% if flash[:notice] %>
      <p class="flash alert alert-success"><%= flash[:notice] %></p>
    <% end %>
    <% if flash[:error] %>
      <p class="flash alert alert-error"><%= flash[:error] %></p>
    <% end %>

    <div class="navbar navbar-fixed-top navbar-inverse">
      <div class="navbar-inner">
          <div class="container-fluid">
          <%= link_to "Condo Motion", posts_path, :class => "brand" %>
          <%= form_tag posts_path, :method => :get, :class => "navbar-search pull-left" do %>
            <%= text_field_tag :search, params[:search], :class => "search-query", :placeholder => "Search", :style => "border-radius: 15px;" %>
          <% end %>
          <ul class="nav pull-right">
            <li><%= link_to current_user.full_name, edit_user_registration_path %></li>
          </ul>
        </div>
      </div>
    </div>
    <div id="main">
      <div id="sidebar">
        <ul class="nav nav-list">
          <li><%= link_to "Posts", posts_path %></li>
          <li><%= link_to "Users", "#" %></li>
        </ul>      
      </div>
      <div id="content">
        <%= yield %>
      </div>
    </div>
    
	</body>
</html>