<!DOCTYPE html>
<html lang="en">
	<head>
    <meta charset="utf-8">
    <title>Condo Motion</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Le styles -->
    <%= stylesheet_link_tag    "application", :media => "all" %>
  	<link href='http://fonts.googleapis.com/css?family=Raleway:100|Varela' rel='stylesheet' type='text/css'>
  	<%= javascript_include_tag "application" %>
  	<%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <!-- Le fav and touch icons -->
    <link rel="shortcut icon" href="../assets/ico/favicon.ico">
    <link rel="apple-touch-icon-precomposed" sizes="144x144" href="../assets/ico/apple-touch-icon-144-precomposed.png">
    <link rel="apple-touch-icon-precomposed" sizes="114x114" href="../assets/ico/apple-touch-icon-114-precomposed.png">
    <link rel="apple-touch-icon-precomposed" sizes="72x72" href="../assets/ico/apple-touch-icon-72-precomposed.png">
    <link rel="apple-touch-icon-precomposed" href="../assets/ico/apple-touch-icon-57-precomposed.png">
  </head>

  <body>
    <% if flash[:notice] %>
      <p class="flash alert alert-success"><%= flash[:notice] %></p>
    <% end %>
    <% if flash[:error] %>
      <p class="flash alert alert-error"><%= flash[:error] %></p>
    <% end %>

    <div id="sites_list">
      <ul class="nav nav-list">
        <% current_user.sites.each do |site| %>
          <li><%= link_to site.name, edit_site_url(site, :subdomain => false) %></li>
        <% end %>
        <li><%= link_to "New Site", new_site_path %></li>
      </ul>
      <div id="user-nav" style="">
        <%= link_to raw('<span class="icon-cog" rel="tooltip" title="Edit Profile"></span>'), edit_user_registration_path %>
        <%= link_to raw('<span class="icon-off" rel="tooltip" title="Sign Out"></span>'), destroy_user_session_path, :method => :delete %>
      </div>
    </div>
    <div id="content_types_list">
      Content Types - Make Partial
    </div>
    <div id="content_stage">
      <%= yield %>
    </div>
    
	</body>
</html>